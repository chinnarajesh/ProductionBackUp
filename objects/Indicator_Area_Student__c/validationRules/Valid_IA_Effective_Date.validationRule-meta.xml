<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Valid_IA_Effective_Date</fullName>
    <active>false</active>
    <errorConditionFormula>AND(
	NOT(
		OR((CONTAINS($Profile.Name, &apos;System Administrator&apos;)),
			(CONTAINS($Profile.Name, &apos;Tier 1 Super Administrators&apos;)))),
 	ISCHANGED(IA_Effective_Date__c)
)</errorConditionFormula>
    <errorDisplayField>IA_Effective_Date__c</errorDisplayField>
    <errorMessage>You do not have access to change the IA Effective Date. Please submit a Service Desk case if you need the IA Effective Date to be changed.</errorMessage>
</ValidationRule>
